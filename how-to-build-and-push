# First time only on virtual environment
pip install --upgrade setuptools wheel
pip install --upgrade twine

# To build
python setup.py sdist bdist_wheel

# To push
python -m twine upload --repository name-of-repo dist/*
